const coffees = ['Capuccino', 'Espresso', 'Vanilla']
// console.log(coffees)

// console.log(coffees.toString());
// console.log(coffees.join(' % '))

// Pop method removes the last element in an array.
// It also Returns the popped out element as a string.
// console.log(coffees.pop()) 

// The push method adds at the end of an array.
// coffees.push('Kahawa')

// The shift method removes the first element, and moves all
// the other elements to a lower index.
// The shift method does return the removed element.
// console.log(coffees.shift());

// The unshift method adds an element at the beginning of an
// array and return the length of the array.
// console.log(coffees.shift('Cappuccino'));

console.log(coffees);